name: Deploy to QA Server

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: Palladium-hub/platform-ci/.github/workflows/backend-qa-ci.yaml@main
    with:
      repo_name: "openmrs-module-kenyaemrorderentry"
    secrets: inherit
